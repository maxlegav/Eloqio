/**
 * Type definitions for Eloquio configuration
 */

export interface EloquioConfig {
  appName: string;
  productName: string;
  bundleIdentifier: string;
  website: string;
  supportEmail: string;
  companyName: string;
  description: string;
  bookingUrl: string;
}
