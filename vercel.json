{
  "buildCommand": "npx turbo run build:vercel --filter=eloquio-web",
  "outputDirectory": "apps/web/dist",
  "installCommand": "rm -rf node_modules && npm ci",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
