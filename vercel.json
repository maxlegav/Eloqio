{
  "buildCommand": "npx turbo run build:vercel --filter=eloquio-web && cp -r apps/web/dist/. dist/",
  "outputDirectory": "dist",
  "installCommand": "rm -rf node_modules && npm ci",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
